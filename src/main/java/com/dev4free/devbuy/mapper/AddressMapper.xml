<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.dev4free.devbuy.mapper.AddressMapper">

<select id="findAddressByUserName" parameterType="String" resultType="Address">
SELECT * FROM address WHERE username = #{username}
</select>

<select id="findAddressById" parameterType="String" resultType="Address">
SELECT * FROM address WHERE id = #{id}
</select>

<insert id="insertShippingAddress" parameterType="Address">
INSERT INTO address(id,username,consignee_name,phone_number,province,city,detail_address) 
VALUES (#{id},#{username},#{consignee_name},#{phone_number},#{province},#{city},#{detail_address});
</insert>

<update id="updateShippingAddress" parameterType="Address">
UPDATE address SET consignee_name=#{consignee_name},
					phone_number=#{phone_number},
					province=#{province},
					city=#{city},
					detail_address=#{detail_address} 
WHERE id=#{id}
</update>


</mapper>